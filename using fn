#include <stdio.h>
int sum(int a,int b)
{ int sum;
    return sum=a+b;
}

int main()
{
    int x,y;
    printf("enter 2 nos");
    scanf("%d%d",&x,&y);
    printf("the sum of two no's is %d",sum(x,y));
    return 0;
}
