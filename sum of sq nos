#include<stdio.h>
int main()
{ int remainder,sum=0,n;
printf("enter a number");
    scanf("%d",&n);
    while(n!=0)
    {
        remainder=n%10;
        sum=sum+remainder*remainder;
        n=n/10;
    }
    printf("result=%d",sum);
}

