#include<stdio.h>
int compare(int a, int b)
{if(a==b)
return 1;
else
return 0;
}
int main()
{
    int x,y,n;
    printf("enter two nos");
    scanf("%d%d",&x,&y);
    n=compare(x,y);
    if(n==1)
        printf("sucess");
    else
        printf("invalid");
    return 0;
}
