#include<stdio.h>
int sum(int n)
{ int remainder,sum=0;
    while(n!=0)
    {
        remainder=n%10;
        sum=sum+remainder;
        n=n/10;
    }
return sum;
}
int main()
{ int n;
    printf("enter a number");
    scanf("%d",&n);
    printf("result=%d",sum(n));
}
